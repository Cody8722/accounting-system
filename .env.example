# 根目錄 docker-compose 用的環境變數（複製為 .env 後填入；.env 已被 gitignore，勿提交）
#
# MONGO_URI：後端容器連現有的 mongodb 容器（在外部 shared-db 網路，主機名用容器名 mongodb）。
# 其餘密鑰（JWT_SECRET / SMTP / 密碼政策）由掛載的 backend/.env 提供，不放這裡。

# ── 現階段：測試庫 ─────────────────────────────────────────────
MONGO_URI=mongodb://accounting_test_user:<在此填入密碼>@mongodb:27017/accounting_db_test?authSource=accounting_db_test

# ── 穩定後切換：正式庫（帳號/密碼/庫名換成正式的，取消註解並覆蓋上面那行）──
# MONGO_URI=mongodb://<正式帳號>:<正式密碼>@mongodb:27017/accounting_db?authSource=accounting_db

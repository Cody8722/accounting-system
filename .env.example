# 根目錄 docker-compose 用的環境變數（複製為 .env 後填入；.env 已被 gitignore，勿提交）
#
# 每個 checkout 各自一份 .env：
#   ~/accounting-system-prod（release） → 指正式庫 accounting_db（用正式 Mongo 帳號）
#   ~/accounting-system-test（develop） → 指測試庫 accounting_db_test
# 主機名用容器名 mongodb（在外部 shared-db 網路）。其餘密鑰（JWT_SECRET/SMTP）由掛載的 backend/.env 提供。
#
# 前置：外部網路需先建立一次（正式/測試/proxy 共用）：
#   docker network create edge

# ── 正式 checkout（~/accounting-system-prod）：accounting_db ──
MONGO_URI=mongodb://<正式帳號>:<正式密碼>@mongodb:27017/accounting_db?authSource=accounting_db

# ── 測試 checkout（~/accounting-system-test）：accounting_db_test（覆蓋上面那行）──
# MONGO_URI=mongodb://accounting_test_user:<密碼>@mongodb:27017/accounting_db_test?authSource=accounting_db_test
